# Sample repo to test self-hosted runner

This repository is a minimal test to verify a self-hosted GitHub Actions runner (ppc64le).

Push this repo to GitHub, register a runner for the repo with the appropriate label (ppc64le),
then push to `main` to trigger the workflow.
